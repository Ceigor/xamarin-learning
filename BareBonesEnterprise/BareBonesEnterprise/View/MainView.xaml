<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModelBase ="clr-namespace:BareBonesEnterprise.ViewModel.Base"
             xmlns:behavior="clr-namespace:BareBonesEnterprise.Behavior"
             x:Class="BareBonesEnterprise.View.MainView"
             xmlns:i18n="clr-namespace:BareBonesEnterprise"
             viewModelBase:LocatorViewModel.AutowireViewModel="true"
             Title="BareBonesEnterprise">
    <StackLayout>
        <Button Text="{i18n:Translate NotesLabel}" Style="{StaticResource MenuButtonStyle}">
            <Button.Behaviors>
                <behavior:EventToCommandBehavior EventName="Clicked" Command="{Binding NavigateToAuthorsCommand}" />
            </Button.Behaviors>
        </Button>
        <Button Text="Categories" Style="{StaticResource MenuButtonStyle}">
            <Button.Behaviors>
                <behavior:EventToCommandBehavior EventName="Clicked" Command="{Binding NavigateToCategoriesCommand}" />
            </Button.Behaviors>
        </Button>
        <Button Text="Quotes" Style="{StaticResource MenuButtonStyle}">
            <Button.Behaviors>
                <behavior:EventToCommandBehavior EventName="Clicked" Command="{Binding NavigateToQuotesCommand}" />
            </Button.Behaviors>
        </Button>
    </StackLayout>

</ContentPage>
